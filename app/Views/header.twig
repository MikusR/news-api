<!doctype html>
<html lang="en">
<head>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }

        nav {
            background-color: #333;
            color: #fff;
            padding: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .flag-buttons {
            display: flex;
            gap: 10px;
        }

        .flag-button {
            background-color: #555;
            color: #fff;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
        }

        ul {
            list-style-type: none;
            display: flex;
            gap: 20px;
        }

        ul li a {
            text-decoration: none;
            color: #fff;
        }

        form {
            background-color: #eee;
            padding: 10px;
            margin: 10px 0;
        }

        label {
            display: flex;
            gap: 10px;
        }

        input {
            padding: 5px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }

        button {
            background-color: #333;
            color: #fff;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
        }

        main {
            padding: 20px;
        }
    </style>
    <script type="module" defer>
        import {polyfillCountryFlagEmojis} from "https://cdn.skypack.dev/country-flag-emoji-polyfill";

        polyfillCountryFlagEmojis();
    </script>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/css/pico.classless.css">

    <title>News for {{ countryFlag }}</title>
</head>
<body>
<nav>
    <div class="flag-buttons">
        <button class="flag-button" onclick="location.href='/country/lv'">🇱🇻</button>
        <button class="flag-button" onclick="location.href='/country/gb'">🇬🇧</button>
        <button class="flag-button" onclick="location.href='/country/us'">🇺🇸</button>
        <button class="flag-button" onclick="location.href='/'">Home</button>
    </div>
    <ul>
        <li><a href="{{ country }}?category=technology">technology</a></li>
        <li><a href="{{ country }}?category=entertainment">entertainment</a></li>
        <li><a href="{{ country }}?category=science">science</a></li>
    </ul>
</nav>
<form action="/search" method="GET">
    <label>
        <input type="search" name="keyword" placeholder="Keyword">
        <input type="text" name="from" placeholder="from">
        <input type="text" name="to" placeholder="to">
    </label>
    <button type="submit">Search</button>
</form>
<main>

